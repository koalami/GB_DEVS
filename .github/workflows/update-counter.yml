name: Update Counter

on:
  workflow_dispatch:
    inputs:
      increment:
        description: "Valor para aumentar el contador"
        required: false
        default: "1"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Increment counter
        run: |
          FILE=contador.json
          VALUE=$(jq '.count' $FILE)
          INCREMENT=${{ github.event.inputs.increment }}
          NEW_VALUE=$((VALUE + INCREMENT))
          echo "Nuevo valor: $NEW_VALUE"
          jq ".count = $NEW_VALUE" $FILE > temp.json && mv temp.json $FILE

          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add $FILE
          git commit -m "Update counter to $NEW_VALUE"
          git push https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }} HEAD:${{ github.ref }}

